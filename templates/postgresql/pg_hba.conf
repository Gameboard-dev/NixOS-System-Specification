# PostgreSQL client authentication (services.postgresql.authentication reads
# this file; lib.mkForce replaces the NixOS module's generated entries).
# Docs: https://www.postgresql.org/docs/current/auth-pg-hba-conf.html

# TYPE  DATABASE        USER            ADDRESS                 METHOD
local   all             all                                     peer
